# TODO

- Verify the local model build on Xcode 26.4
- Measure ASR latency on device for each preset
- Tune the Whisper buffer and silence thresholds
- Add model checksum validation if you want stricter downloads
- Optionally add local neural TTS instead of system TTS

